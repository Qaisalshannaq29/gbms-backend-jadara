server:
  port: 8030

spring:
  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=GPMS2;encrypt=false;trustServerCertificate=true
#
    username: sa
    password: A126109a@@
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    database-platform: org.hibernate.dialect.SQLServerDialect
    hibernate:
      ddl-auto: update  # options: update, validate, create, create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG
